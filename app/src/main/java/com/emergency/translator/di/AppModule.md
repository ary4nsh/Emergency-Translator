# AppModule.kt

Placeholder Hilt module for additional app-wide singleton dependencies.

## Object: `AppModule`

`@Module` installed in `SingletonComponent`. Currently empty; reserved for future dependency providers.
